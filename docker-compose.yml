services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - openclaw-data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  openclaw-data:
